---
const objectives = [
  {
    number: "1",
    img: "../../wizard/12.png",
    text: "Design et Branding de la marque",
  },
  {
    number: "2",
    img: "../../wizard/13.png",
    text: "Performance du site Web",
  },
  {
    number: "3",
    img: "../../wizard/14.png",
    text: "Version Mobile du site Web",
  },
  {
    number: "4",
    img: "../../wizard/15.png",
    text: "Je ne sais vraiment pas",
  },
];
---

<style>
  .PillList-item {
    cursor: pointer;
    /* display: inline-block; */
    /* float: left; */
    font-size: 14px;
    font-weight: normal;
    line-height: 20px;
    margin: 0 12px 12px 0;
    text-transform: capitalize;
  }

  .PillList-item input[type="checkbox"] {
    display: none;
  }
  .PillList-item input[type="checkbox"]:checked + .PillList-label {
    background-color: #ffbff9;
    border: 2px solid #ffbff9;
    color: #14171f;
    /* padding-right: 16px;
    padding-left: 16px; */
  }
  .PillList-label {
    border: 2px solid #fff;

    color: #fff;
    display: block;
    padding: 14px 28px;
    text-decoration: none;
  }
  .PillList-item
    input[type="checkbox"]:checked
    + .PillList-label
    .Icon--checkLight {
    display: inline-block;
  }
  .PillList-item
    input[type="checkbox"]:checked
    + .PillList-label
    .Icon--addLight,
  .PillList-label .Icon--checkLight,
  .PillList-children {
    display: none;
  }
  .PillList-label .Icon {
    width: 12px;
    height: 12px;
    margin: 0 0 0 12px;
  }
  .Icon--smallest {
    font-size: 12px;
    line-height: 12px;
  }
  .Icon {
    background: transparent;
    display: inline-block;
    font-style: normal;
    vertical-align: baseline;
    position: relative;
  }
</style>

<div class="">
  <div class="px-4">
    <h1 class="text-white font-bold text-3xl mb-4">Défis actuels</h1>
    <p class="opacity-50 text-white mb-12 text-xl">
      A quel niveau rencontrez-vous des difficultés à présent ?
    </p>
  </div>
  <div class="" id="wrapper">
    <div class="">
      {
        objectives.map((item, index) => (
          <label class="PillList-item">
            <input
              type="checkbox"
              name="feature"
              value={item.number}
              id={item.number}
            />
            <span class="PillList-label rounded-full font-semibold">
              <div class="flex justify-between">
                <>
                  <div class="my-auto text-4xl w-2/12">
                    <img src={item.img} class="  shrink-0" />
                  </div>
                  <p class="text-lg my-auto pl-6 font-bold w-10/12">
                    {item.text}
                  </p>
                </>
              </div>
            </span>
          </label>
        ))
      }
    </div>
  </div>
</div>
